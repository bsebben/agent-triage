// src/tabs/defaults.js — Default config for each tab module
// Imported by config.js for merging, and by tab modules for reference.

export const loops = {
  enabled: true,
  dataDir: null,
  installUrl: "https://silver-adventure-o3qwg53.pages.github.io/plugin.html?name=claude-loops",
};

export const tickets = {
  enabled: true,
};

export const pulls = {
  enabled: true,
  orgFilter: null,
};
